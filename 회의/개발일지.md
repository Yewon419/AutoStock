# AutoStock 개발일지

---

## 2026-05-15

### 포폴 봇 risk_params 튜닝 (DB-only, 코드 변경 없음)

5/6~5/15 (10일) 포폴 4봇 성과 감사 결과 기반 운영 튜닝.

#### 진단

| 봇 | 거래 | 승률 | SELL 평균 | 누적 P&L | 최고/최저 | 보유 |
|---|---|---|---|---|---|---|
| 19 포폴_눌림목 (str 47) | 24 | **60.0%** | **+7.37%** | **+108,771** | +42.1% / −5.2% | 4종목 |
| 20 포폴_변동성압축 (str 48) | 26 | 25.0% | +0.13% | −12,861 | +23.3% / −7.6% | 2종목 |
| 21 포폴_과매도단타 (str 49) | 117 | 12.3% | −0.56% | −31,325 | +4.9% / −2.2% | 3종목 |
| 22 포폴_돌파단타 (str 50) | 38 | 33.3% | −0.13% | −2,449 | +2.8% / −1.4% | 2종목 |

- **bot_19**: 4봇 중 유일한 흑자축. 한도(60%)를 4/4 풀로 채우는 중 → 한도 자체가 작은 게 병목
- **bot_20**: BUY 14 vs SELL 12 = swing인데 회전이 빠름. 동시보유 2/4 = 신호 부족 아닌 **조기 청산**이 원인. self-review에서도 RSI 45~62 상단 좁음, vol_ratio 1.3 권장 1.5, signal_ticker_pct 99%(선별력 약함) 지적
- bot_21/22 (scalping): 자기 한도(30%/20%)를 풀로 채움 = idle cash는 설계상 안전마진

#### 변경

| bot_id | 항목 | 변경 전 | 변경 후 | 의도 |
|---|---|---|---|---|
| 19 | position_size_pct | 15 | **20** | 흑자축 비중 확대 |
| 19 | max_positions | 4 | **5** | 활용한도 60% → 100% |
| 20 | stop_loss_pct | 3.5 | **5.0** | swing normal pullback 허용 |
| 20 | take_profit_pct | 8.0 | **12.0** | 추세 더 길게 끌고 |
| 20 | max_drawdown_pct | 10 | **12** | SL 완화에 따른 가드 동반 완화 (4종목 동시SL 3% 자산 손실, MDD 가드까지 충분한 여유) |

bot.initial_cash × position_size_pct로 종목당 예산 계산. `bot_engine.run_all_bots`가 매 사이클 DB 재조회라 hot-update 안전.

#### 관찰 포인트 (5/16~22)

- bot_19: 5번째 종목 진입, 누적 거래 30건 임계 도달 (현재 24건, +6건 필요)
- bot_20: 동시보유 3~4 도달 / 승률 25% → 35%+ 개선 / MDD 12% 가드 발동 여부
- 1주 후 bot_20 미개선 시 — 신호률이 아니라 **종목 선별력 (str 48 signal_ticker_pct 99%)** 의심 → 재생성/폐기 고려

### 봇 단위 strategy·캔버스 분리 — Phase 0 마이그레이션

운영 흐름 변경: "전역 strategy/canvas → 봇 1:1 보유". 봇 페이지 안에서 그 봇 전용 AI 팔레트로 strategy 생성·튜닝하는 모델로 전환. 상세 진행 트래커는 [14_봇_캔버스_분리.md](14_봇_캔버스_분리.md) 참조.

#### Phase 0 작업

- 53개 고아 strategy DELETE (봇에 연결 안 된 ai_generated 누적분)
- `strategies.bot_id` FK 컬럼 추가 (NOT NULL, UNIQUE, ON DELETE CASCADE) — 1:1 강제
- 5개 잔존 strategy 자동 매핑: 34→bot_16, 47→bot_19, 48→bot_20, 49→bot_21, 50→bot_22
- 신규 테이블 2개:
  - `strategy_history` — 변경 이력·undo·감사 로그
  - `tuning_suggestions` — 08:30 자동 제안함
- SQLAlchemy 모델 갱신 (`Strategy.bot_id`, 신규 `StrategyHistory`, `TuningSuggestion`)
- pg_dump 백업 (`backups/phase0_20260515_114010.sql`, gitignored)

#### 사전 점검 / 후처리

- 봇 19~22 일시 STOPPED → 마이그레이션 → RUNNING 복원 (튜닝 값 정상 보존)
- Redis 캔버스 5개 키 (user_id=1) — Phase 0에선 손 안 댐, Phase 4 정리에서 결정
- 백엔드·celery 4컨테이너 재기동, 헬스 200 OK

#### Phase 1 완료 — 백엔드 봇 단위 전환

같은 날 4 sub-phase 모두 끊어 진행.

| Sub | 내용 | Commit |
|---|---|---|
| 1A | `create_bot` 빈 strategy 동반 트랜잭션 | `0dcfeae` |
| 1B | `api/bot_canvas.py` — apply-diff·undo·history·suggestions·dismiss | `608c125` |
| 1C | LLM 대화 어시스턴트 + 자동 진단 (chat / ai-generate) | `cffc87a` |
| 1D | `tasks/bot_tuning.py` + 08:30 봇 단위 제안 beat | `39d5e7f` |

기존 `llm-generate-strategy` / `reopen-pending-patterns` beat 제거. 함수 자체는 Phase 4에서 폐기.

#### Phase 2 완료 — 프론트 봇 페이지 탭화

같은 날 이어서 진행.

| Sub | 내용 | Commit |
|---|---|---|
| 2A | `BotDetailView` 캔버스 탭 + `BotCanvas` 스켈레톤 | `658d4b6` |
| 2B | AI 어시스턴트 대화 + diff + 적용/되돌리기 | `914e5a7` |
| 2C | 변경 이력 + 제안함 표시 | `850d1a3` |
| extra | 노드 편집기 임베드 (CanvasView 통째 — 봇 단위 격리는 Phase 4) | `40daa92` |

봇 페이지 [캔버스] 탭이 기본 활성 탭. 좌: 현재 상태, 우: AI 어시스턴트, 하단: 제안함·이력·노드 편집기.

#### Phase 3 완료 — 봇 생성 흐름 + 헤더 알림함

| Sub | 내용 | Commit |
|---|---|---|
| 3A | 봇 생성 후 `/bots/{id}` 자동 이동 (캔버스 탭 진입) | `018e64b` |
| 3B | 헤더 🔔 배지 + 봇별 드롭다운 (`pending-summary` API, 30s 폴링) | `018e64b` |

---

## 2026-03-06

### 완료 작업

#### Phase 2: 데이터 파이프라인
- **pykrx 자동 수집** (`tasks/collect.py`)
  - `collect_all_stocks`: 전체 2,770개 종목 일봉 + 기술지표 수집
  - `collect_missing_data`: 누락 데이터 자동 보완 (평일 09:00 + worker 시작 시)
- **자동 실행 설정**
  - Docker `restart: always` (postgres, redis, celery-worker, celery-beat)
  - Windows Task Scheduler `AutoStock-Collector` 등록 (로그인 시 자동 시작)

#### Phase 3: 전략 + 백테스트
- 전략 CRUD API (`api/strategies.py`)
- 백테스트 엔진 (`services/backtest_engine.py`)
- 전략 프리셋 5개 추가 (StrategyView.vue)
  - RSI 과매도, RSI 과매수 모멘텀, MACD 골든크로스, 스토캐스틱 반전, 강세장 눌림목

#### Phase 4: 자동매매 Mock
- bot_engine: 5분마다 run_all_bots 실행
- 손절/익절/최대낙폭 체크
- 전체 파이프라인 검증: 삼성전자 9주 매수 → +10.5% 익절 확인

#### Phase 4.5: AI 분석 탭
- **ML 종목 스코어링** (`tasks/ai_tasks.py` - `train_and_score`)
  - RandomForest Classifier (트리 100개, 최대 깊이 6)
  - 피처: RSI, MACD 히스토그램, Stoch K/D, ADX, MA 기울기, ATR, 볼린저밴드 위치
  - 레이블: 5거래일 후 수익률 > 1% → 매수 신호
  - 학습 결과: 296,472 샘플 → 2,655종목 예측 → 상위 50개 Redis 저장
  - 매일 17:30 자동 실행 (Celery Beat)
- **전략 파라미터 최적화** (`tasks/ai_tasks.py` - `optimize_strategy`)
  - 지정 indicator/condition의 value 범위를 Grid Search
  - 고거래량 상위 100개 종목 백테스트
  - 최대 30단계, 샤프 비율 기준 최적값 도출
- **프론트엔드** (`frontend/src/views/AiView.vue`)
  - ML 스코어링 탭: 점수 테이블 + 진행률 바 + 수동 실행 버튼
  - 파라미터 최적화 탭: 설정 폼 + SVG 라인 차트 + 결과 테이블
- **전략 스캐너** (`tasks/scanner.py`)
  - 매일 17:00, RUNNING 봇의 전략 조건을 전체 종목 적용
  - 거래량 상위 최대 30개 종목으로 bot.tickers 자동 갱신

### 현재 DB 현황
- 종목 수: 2,770개 (KOSPI + KOSDAQ)
- 가격 데이터: ~666,000건
- 기술지표 데이터: ~638,000건

### 기술 이슈 및 해결
| 이슈 | 해결 |
|------|------|
| lightweight-charts v5 API 변경 | `addCandlestickSeries` → `addSeries(CandlestickSeries, ...)` |
| Docker 서비스별 이미지 분리 | `backend`, `celery-worker`, `celery-beat` 각각 별도 빌드 필요 |
| scikit-learn 설치 캐시 문제 | `docker compose build --no-cache celery-worker celery-beat` |

---

### 다음 작업 예정

- [ ] Phase 5: KIS(한국투자증권) API 연동
  - WebSocket 실시간 시세
  - REST API 주문 연결
  - `broker/kis_broker.py` 구현
- [ ] AI 탭 개선 (선택)
  - 스코어 상위 종목을 봇 tickers에 직접 적용하는 버튼
  - 모델 성능 지표 표시 (정확도, AUC 등)
  - 특성 중요도(feature importance) 시각화

---

## 2026-03-06

### 완료 작업

#### Phase 5.5: 봇 보고서 개선
- **체결 내역 API** (`GET /bots/{id}/executions`) 신규
- **성과 통계 API** (`GET /bots/{id}/performance`) 신규 — 샤프비율, MDD, Profit Factor 등
- **BotDetailView** 전면 개편: 성과 카드 6개, 체결 탭, 누적손익+일일손익 차트
- **BotView** 개선: 봇 카드 UI 개선, 현재 포지션 요약 표시
- **전략 스캐너** (`tasks/scanner.py`) 개선: candle_interval 반영, 단타봇 ticker 스캔 분리

#### Phase 6: 단타 자동매매 완성
- **`tasks/intraday_collector.py`** 신규
  - 평일 09:00~15:00 매 1분 실행 (Celery Beat)
  - RUNNING 상태 scalping 봇의 ticker별 분봉 수집 (KIS API 또는 Mock)
  - RSI(14), MACD(12,26,9), Bollinger Bands(20,2), MA(5,10,20), 거래량비율, 시가대비등락 계산
  - Mock 캔들: DB 실제 종가 기준, 횡보→하락→거래량 급증(50% 확률) 3단계 패턴
  - 지표 Redis 저장: `rt:ind:{ticker}:{interval}` (TTL 8시간)
- **`tasks/scalping_engine.py`** 신규
  - 평일 09:00~15:00 매 1분 실행
  - 분봉 지표(Redis) 기반 매수 신호 평가
  - 손절/익절/당일 강제 청산 처리
  - 지원 조건: above/below/between/golden_cross/dead_cross
- **`tasks/bot_engine.py`** 수정
  - `run_all_bots`에서 `bot_type='scalping'` 봇 제외 → swing 엔진 간섭 차단
- **전략 타입 분리**
  - `Strategy.strategy_type` 컬럼 추가 (swing | scalping)
  - API 스키마, 서비스 레이어 모두 반영
- **`frontend/src/views/StrategyView.vue`** 전면 개편
  - 전략 타입 탭 (전체/스윙/단타) 필터
  - 지표 목록 분리: 스윙 14개(일봉), 단타 12개(분봉)
  - 단타 프리셋 5개 추가: 과매도 거래량 반등, MACD 0선 돌파, 거래량 급증 모멘텀, 갭업 모멘텀, MACD 히스토그램 전환

### 기술 이슈 및 해결
| 이슈 | 해결 |
|------|------|
| Mock 캔들 RSI 중립 고착 (40~65) | 3단계 패턴 설계: 횡보→하락→거래량 급증으로 RSI<35 + volume_ratio>1.5 구현 |
| bot_engine이 scalping 포지션 MDD 오평가 | run_all_bots에 `bot_type != 'scalping'` 필터 추가 |
| Mock 가격 괴리 (-96~-99%) | DB 실제 종가를 ref_price로 전달, fallback 50,000→1,000원 수정 |
| Redis 구 캐시로 새 로직 미적용 | mock 모드에서 _merge_candles 건너뛰고 매 분 새 캔들 생성 |
| 지표 키 이름 불일치 (bb_* vs bollinger_*) | bollinger_upper/middle/lower로 통일 |

### 현재 상태
- 단타 테스트봇(id=10): RUNNING, 1분 주기 거래 중
- Mock 모드: DB 종가 기준 매수, 분봉 지표 정상 계산 (RSI 2~30, volume_ratio 2~30)
- 다음 과제: KIS 실전 소액 주문 검증 (Phase 7)
